Sources in `huffman/`, `painting/`, `sets/` , `simple/` and
`symderiv/` directories are compatible with Guile. You'll need Cairo
bindings to make code in `painting/` work.

Code in `generic-ops/` is compatible with PLT implementation.
SchemeUnit is used for tests.